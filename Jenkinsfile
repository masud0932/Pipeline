pipeline {
    agent any

 tools {
maven 'Maven'
         }

    stages {
        stage('Initialize') {
            steps {
               sh '''

            }
        }

        stage('Build with Maven') {
            steps {
                sh 'mvn clean package'
            }
        }

        
    }
}
